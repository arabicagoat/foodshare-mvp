# Example environment config for FoodShare server

# PostgreSQL connection string
DATABASE_URL=postgres://youruser:yourpassword@localhost:5432/foodshare_okc

# Server port
PORT=3000

# Session secret / JWT secret (later)
SESSION_SECRET=change_this_to_a_random_string
